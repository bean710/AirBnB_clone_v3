Get one City by id
---
parameters:
  - name: city_id
    in: path
    type: string
    required: true
responses:
    200:
        description: A state object
        schema:
            $ref: '#/definitions/City'
    404:
        description: City not found with that id
